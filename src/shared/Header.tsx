import styles from '../styles/Home.module.css';

const Header = () => {
  return (
    <div className={styles.header}>
        <span className={styles.headerSpan}>HACKER RANKS</span>
    </div>
  )
}

export default Header